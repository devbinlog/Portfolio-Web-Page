{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install && cd packages/types && pnpm exec tsc && cd ../utils && pnpm exec tsc && cd ../../apps/api && pnpm exec prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "pnpm exec prisma migrate deploy --schema=apps/api/prisma/schema.prisma && node apps/api/dist/main.js",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
